import { FC } from "react";
import TitleBox from "../../components/TitleBox";

const BlogPage: FC = () => {

    return (
        <div>
            <TitleBox/>
        </div>
    );
};

export default BlogPage;